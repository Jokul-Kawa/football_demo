{
  "name": "world-cup-2026-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "2026 World Cup assistant MVP for China mainland users.",
  "workspaces": [
    "apps/api"
  ],
  "scripts": {
    "start": "npm --workspace apps/api start",
    "dev:api": "npm --workspace apps/api dev",
    "test": "npm --workspace apps/api test"
  },
  "engines": {
    "node": ">=20"
  }
}

